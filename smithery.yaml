# Generated by https://smithery.ai
startCommand:
  type: stdio
  configSchema:
    type: object
    required:
      - homeAssistantToken
      - homeAssistantUrl
    properties:
      homeAssistantToken:
        type: string
        description: Long-Lived Access Token for authenticating with Home Assistant
        title: Home Assistant Token
      homeAssistantUrl:
        type: string
        description: URL of your Home Assistant instance (e.g., http://homeassistant.local:8123)
        title: Home Assistant URL
        pattern: '^https?://.*'
      debug:
        type: boolean
        description: Enable debug logging
        title: Debug Mode
        default: false
      requestTimeout:
        type: number
        description: Request timeout in milliseconds
        title: Request Timeout
        default: 10000
        minimum: 1000
        maximum: 60000
  commandFunction: |
    (config) => ({
      command: 'node',
      args: ['dist/index.js'],
      env: {
        HOME_ASSISTANT_TOKEN: config.homeAssistantToken,
        HOME_ASSISTANT_URL: config.homeAssistantUrl,
        DEBUG: config.debug ? 'true' : 'false',
        REQUEST_TIMEOUT: config.requestTimeout ? config.requestTimeout.toString() : '10000'
      }
    })
  exampleConfig:
    homeAssistantToken: eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...
    homeAssistantUrl: http://homeassistant.local:8123
    debug: false
    requestTimeout: 10000